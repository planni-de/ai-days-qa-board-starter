# Vercel Postgres — auto-provisioned by `vercel link`
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Upstash Redis — for rate limiting + AI clustering cache
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# AI Gateway — Claude Haiku is enough for clustering
AI_GATEWAY_API_KEY=

# Admin Basic Auth — pick something long and random
ADMIN_PASSWORD=

# Public site URL, used by the QR generator and OG meta
NEXT_PUBLIC_SITE_URL=https://ai-days.planni.de
